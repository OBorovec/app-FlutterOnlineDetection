name: FlutterOnlineDetection
description: Mobile application real-time object detectio.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  camera: ^0.5.8+11
  tflite: ^1.1.1
  tflite_flutter: ^0.5.0
  tflite_flutter_helper: ^0.1.2
  image: ^2.1.12

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/mobilenet_v1_1.0_224_quant/model.tflite
    - assets/mobilenet_v1_1.0_224_quant/labels.txt
